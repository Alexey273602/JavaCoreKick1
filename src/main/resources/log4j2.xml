<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">
    <Properties>
        <property name="LOG_DIR">logs</property>
        <property name="pattern_file">%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</property>
    </Properties>

    <Appenders>
        <!-- Консоль -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${pattern_file}"/>
        </Console>

        <!-- Файл с ротацией -->
        <RollingFile name="FileRolling"
                     filename="${LOG_DIR}/text_log.log"
                     filePattern="${LOG_DIR}/archive/%d{yyyy-MM-dd_HH-mm}_%i.log"
                     append="true">
            <PatternLayout pattern="${pattern_file}"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="1MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Логи пакета -->
        <Logger name="by.kozhevnikov.task1" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileRolling"/>
        </Logger>

        <!-- Root Logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileRolling"/>
        </Root>
    </Loggers>
</Configuration>